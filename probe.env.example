# Copy to probe.env (gitignored) on the Pi. The probe WiFi password lives only in
# the saved nmcli profile named here, never in a file.
PROBE_CON=DIRECT-PALHD3012406A0356
PROBE_IP=192.168.1.1
PROBE_PORT=5828
